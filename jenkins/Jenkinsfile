@Library("shared-Library")_

pipeline{
    agent any
    // tools {nodejs "Node"}

    stages {
        /* groovylint-disable-next-line UnnecessaryGString */
        stage("Checkout target branch") {
            steps {
                checkout scm
            }
        }
        /* groovylint-disable-next-line UnnecessaryGString */
        stage ("build image") {
            steps {
                buildImage()
            }
        }
        // stage ("publish image") {
        //     steps {
        //         sh ''
        //     }
        // }
    }
}
